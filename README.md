# Skill-Compass

Navigating Australia's Data Analytics Job Market Through Skill Intelligence

## Local development

Install Python 3.12 and [uv](https://docs.astral.sh/uv/), then create the local
environment and install the development dependencies:

```shell
uv sync
```

Run the package checks with:

```shell
uv run pytest
uv run ruff check .
```
