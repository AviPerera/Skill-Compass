"""Expose environment configuration for application boundaries.

This package loads settings for explicit use cases and must not start external
services, expose secret values, or make processing depend on collection secrets.
"""
